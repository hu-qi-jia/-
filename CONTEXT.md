# 拼多多客服快捷回复工具(Personal AI Memory 改造)

单人客服在本地 Chrome 中使用的扩展:捕获拼多多商家后台聊天页的买家问答,沉淀为可向量检索的回复候选;客服把优秀回复提升为金标准,在后续同类问题上一键召回填充。

## Language

**买家问题(问题)**:
客服首次回复之前,会话中一段连续的买家文本消息合并而成的整体。它是检索的查询对象,也是金标准的"问题"字段来源。
_Avoid_: 买家消息(单条)、query

**客服回复(回复)**:
一个买家问题之后,客服在浏览器聊天页发出的单条文本消息。同一条回复可能对应多个买家问题(内容被重复使用);同一个问题下可挂多条客服回复(同问多答)。
_Avoid_: 回答、话术(模板语义不符)

**问答记录**:
一次捕获的结果单元 = 一个买家问题 + 挂在其下的全部客服回复。仅存在于保留期内,是"历史回答"检索层的内容。
_Avoid_: 记忆记录(仅作 UI 列表名)、对话记录

**会话**:
与某位买家的整段聊天,平台侧有唯一标识。捕获、去重、归并都以会话为边界;会话内按时间线切分为"问题段 / 回复段"交替。
_Avoid_: 对话、聊天线程

**回填**:
客服打开(激活)一个会话时,把页面加载出的历史消息按相同规则分段入库。回填消息超过保留期的跳过,全部按消息幂等去重。
_Avoid_: 导入(用户主动的备份恢复动作)

**金标准**:
客服主动把某条(组)回复连同其问题提升出的"标准问答"文档(问题字段 + 回复字段),可编辑、可删除、可归入回复文件夹,不受保留期约束。提升是复制,不改变原问答记录。
_Avoid_: 收藏回复、标准答案、模板

**回复文件夹**:
金标准的分类树,两层结构,根层预设"未分类"。文件夹只收金标准,不收问答记录。
_Avoid_: prompt 文件夹(原项目遗留命名)

**知识库**:
(v1 未启用)客服自行上传的 md 文档集合,整库分块向量化,作为独立检索层参与回复召回与直接填充排序。
_Avoid_: 文档库、资料

**AI 回复按钮**:
买家每条文本消息旁常显的按钮;点击后以该条消息所属的买家问题为查询,召回回复候选。
_Avoid_: Recall 按钮(原项目命名)、魔法按钮

**候选回复**:
一次检索召回、可能被填充的回复文本及其来源(金标准 / 历史回复 / 未来知识库)。内容相同的候选(归一化后)折叠为一条。
_Avoid_: 结果、命中

**候选弹窗**:
同一问题存在多条不同候选回复时弹出的 top-k=3 选择器,金标准置顶展示;弹窗内可直接将候选提升为金标准。
_Avoid_: 弹框、modal

**直接填充**:
设置开关开启后的行为:不再弹窗,按 金标准 > 知识库 > 历史回复 的优先级把最高匹配候选填入输入框;相似度低于阈值时不填充。填充只写输入框,由客服本人点发送,不替客服发言。
_Avoid_: 自动回复(语义为自动发送)

**保留期**:
问答记录自买家问题产生起的存续期限,默认 90 天可调(30–365)。金标准与文件夹不受保留期约束。
_Avoid_: TTL、过期(实现用词)

**来源标注**:
填充发生或候选展示时,对客服明示该文本出自金标准 / 知识库 / 历史回复,供其判断可信度。
_Avoid_: 引用、出处

**买家标识**:
随问答记录存储的买家 uid 尾号与会话标识,仅用于会话内去重与未来筛选,不作为检索维度,不对外展示。
_Avoid_: 买家信息、买家资料

**未分类**:
回复文件夹的根层预设文件夹,所有新提升的金标准默认归入其中。
_Avoid_: 默认文件夹
